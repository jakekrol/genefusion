# goal

understand which bam files from ICGC i have access to in EGA, but are yet to be indexed.

# info

ega_datasets.txt was gathered by copying the list of IDs from the webbrowser page https://profile.ega-archive.org/datasets/ (logged in)

# result

- for the file ids that were resolved, we have already indexed all but 3.
- the mapping of ega_run_id to ega_file_id is one to many, so it is not a reliable translation
