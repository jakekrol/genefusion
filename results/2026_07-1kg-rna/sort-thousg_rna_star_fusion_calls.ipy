# coding: utf-8
import pandas as pd
from polymerization.stix2fusion import *
df=pd.read_csv("thousg_rna-star_fusion_calls.tsv",sep="\t")
df
df['gene_x'] = df['fusion'].apply(lambda x: x.split('--')[0])
df['gene_y'] = df['fusion'].apply(lambda x: x.split('--')[1])
df
order = ['gene_x', 'gene_y', 'sample']
df[order]
df_sub =df[order]
df_sub
df_sub
from polymerization.io import *
df_bed=read_bed("/data/jake/genefusion/results/2026_09-living_bed/grch37.genes.sort.bed", gene_col_idx=3)
df_bed.shape
df_sort=left_sort_fusion_set(df_sub,df_bed)
df_sort
mask=df_sort['gene_left']=='TIMM23'
df_sub[mask]
df_sub[mask].head(40)
df_sub.to_csv("x", sep="\t", index=False)
df_sub[mask].to_csv("x",sep="\t",index=False)
mask = df_sub['gene_left'] == 'PARGP1'
df_sub[mask]
df
df_sort
df_sort.to_csv('thousg_rna-star_fusion_calls.sort.tsv',sep="\t",index=False)
get_ipython().run_line_magic('', 'save sort-thousg_rna_star_fusion_calls.ipy')
